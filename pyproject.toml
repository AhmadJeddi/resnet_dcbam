[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "resnet-dcbam"
version = "0.1.0"
description = "ResNet50 backbone with DCBAM attention (channel + spatial)."
license = { text = "MIT" }
requires-python = ">=3.8"
